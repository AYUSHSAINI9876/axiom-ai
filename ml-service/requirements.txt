# Pinned for reproducible builds. LlamaIndex ships breaking changes on minor
# releases, so the core package is pinned alongside every integration package
# that imports from it.

# Web framework
fastapi==0.139.2
uvicorn==0.51.0
python-multipart==0.0.32
python-dotenv==1.2.2

# LlamaIndex — packages this service imports directly
llama-index==0.14.16
llama-index-core==0.14.23
llama-index-embeddings-huggingface==0.7.0
llama-index-vector-stores-qdrant==0.10.2
llama-index-llms-ollama==0.10.1
llama-index-llms-groq==0.5.0
llama-index-readers-file==0.5.6
llama-index-retrievers-bm25==0.7.1

# LlamaIndex — transitive packages pulled in by the `llama-index` meta-package.
# Pinned deliberately: the meta-package leaves these unconstrained, and pip then
# backtracks through dozens of llama-cloud and
# llama-index-indices-managed-llama-cloud releases trying to find a compatible
# set, which added ~20 minutes to a clean install.
llama-index-cli==0.5.5
llama-index-embeddings-openai==0.5.2
llama-index-indices-managed-llama-cloud==0.11.1
llama-index-instrumentation==0.5.0
llama-index-llms-openai==0.6.26
llama-index-llms-openai-like==0.5.3
llama-index-readers-llama-parse==0.6.1
llama-index-workflows==2.22.2
llama-cloud==1.6.0
llama-parse==0.5.20

# Deep learning runtime. `==2.13.0` also matches the `2.13.0+cpu` build that the
# Docker image installs from PyTorch's CPU index (PEP 440 ignores the local
# version segment when the specifier omits it), so this pin holds in both.
torch==2.13.0

# Retrieval / embeddings / storage
qdrant-client==1.18.0
sentence-transformers==5.6.1
rank-bm25==0.2.2
pypdf==6.14.2
